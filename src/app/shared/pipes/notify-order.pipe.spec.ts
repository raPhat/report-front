import { NotifyOrderPipe } from './notify-order.pipe';

describe('NotifyOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new NotifyOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
