import { MediumEditorDirective } from './medium-editor.directive';

describe('MediumEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new MediumEditorDirective();
    expect(directive).toBeTruthy();
  });
});
